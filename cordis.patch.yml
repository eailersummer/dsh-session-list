# dsh-session-list bundle patch: inserts the client-only plugin row into the
# web profile roster. The node half (exports ".") is an empty Cordis plugin;
# the `dsh.client` declaration in package.json makes the browser half
# (exports "./client") load in the web GUI.
- insert:
    - id: session-list
      name: 'dsh-session-list'
